<a class="btn btn-primary mt-3" href="/">Back</a>
<div class="container mt-3 mb-3">
  <h1>DC List</h1>
  <a href="/toy/dc/add" class="btn btn-success">Add</a>
  <a
    onclick="return confirm('Are you sure you want to delete all')"
    href="/toy/dc//deleteall"
    class="btn btn-danger"
  >Delete all</a>
  <table border="1">
    <tr>
      <th colspan="7">Dc List</th>
    </tr>
    <tr>
      <th>Image</th>
      <th>Brand Name</th>
      <th>Model Name</th>
      <th>Description</th>
      <th>Price</th>
      <th>Quantity</th>
      <th>Menu</th>
    </tr>
    {{#each dc}}
      <tr>
        <th><img src="{{ image }}" width="100" height="150"></th>
        <th>{{brandName}}</th>
        <th>{{modelName}}</th>
        <th>{{description}}</th>
        <th>${{price}}</th>
        <th>{{ quantity }}</th>
        <th>
            <a href="/toy/dc/edit/{{ _id }}" class="btn btn-warning">Edit</a> 
            <a href="/toy/dc//delete/{{ _id }}" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this toy?')">Delete</a>
        </th>
      </tr>
    {{/each}}

  </table>
</div>